## Installation
```bash
pip install git+https://github.com/melvinthoompunkal/claude-filing-mem.git
```

Then add it to Claude Code:
```bash
claude mcp add claude-filing-mem claude-filing-mem
```

Then set your API key in a `.env` file in your home directory:
```
ANTHROPIC_API_KEY=your_key_here
```